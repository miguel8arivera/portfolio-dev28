{
  "name": "portfolio-dev",
  "version": "2.0.0",
  "description": "Modern portfolio with TypeScript backend",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "echo '=== tsc version ===' && npx tsc --version && echo '=== Running tsc ===' && tsc && echo '=== tsc OK, checking dist ===' && ls -la dist/ && echo '=== Installing client deps ===' && npm install --prefix client && echo '=== Building client ===' && npm run build --prefix client && echo '=== BUILD COMPLETE ==='",
    "server": "tsx watch src/server.ts",
    "server:prod": "node dist/server.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "npm run server:prod",
    "heroku-postbuild": "npm run build && NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  },
  "author": "Miguel Ochoa",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.1",
    "helmet": "^8.1.0",
    "nodemailer": "^6.7.2",
    "nodemon": "^2.0.15",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.1",
    "@types/nodemailer": "^7.0.9",
    "tsx": "^4.21.0"
  }
}
